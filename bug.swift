let arr = [1, 2, 3]
let lastElement = arr.last
//The compiler may not detect errors during development if an array is accessed out of bounds, especially with optional unwrapping, leading to unexpected crashes during runtime.